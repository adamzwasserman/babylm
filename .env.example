# Copy this file to .env and adjust if needed.
#   cp .env.example .env
#
# build_corpus.sh and any scripts that talk to childes-db will source .env
# automatically when present. .env is gitignored.

# CHILDES read-only MySQL access (publicly documented, read-only).
# Source: https://childes-db.stanford.edu (the DNS name sometimes fails to
# resolve from non-academic networks; the IP below is the canonical host).
export CHILDESDB_HOST=52.37.132.215
export CHILDESDB_USER=childesdb
export CHILDESDB_PASSWORD=uy5z4hf7ihBjf
export CHILDESDB_DATABASE=2021.1
